# Personal data — volume-mounted at runtime, never baked into the image
config.yml
icons/

# Build artifacts
node_modules/
dist/
.astro/

# Docker / Compose files (not needed inside the image)
.dockerignore
docker-compose*.yml

# Dev / editor
.env
.env.*
*.local
.DS_Store
.idea/
*.log

# Git
.git/
.github/
